import * as React from 'react'
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/assessment')({
  component: AssessmentPage,
})

function AssessmentPage() {
  const content = `'''Do not make any visual modifications. The phrases I write are commands to understand what I want, not to be written down. Understand their content well, then execute what is required.'''\n                                        \n                                            \n                                            Continue the GoRebalance ASSESSMENT FLOW. Build the integrations and the shared form primitives. No step UI yet.\n\n1) src/lib/firebase.ts — initialise the app, export db (Firestore) and storage. Read config from env: VITE_FIREBASE_API_KEY, VITE_FIREBASE_AUTH_DOMAIN, VITE_FIREBASE_PROJECT_ID, VITE_FIREBASE_STORAGE_BUCKET, VITE_FIREBASE_MESSAGING_SENDER_ID, VITE_FIREBASE_APP_ID, VITE_FIREBASE_MEASUREMENT_ID. Guard re-initialisation with getApps().length.\n\n2) src/lib/cloudinary.ts — export uploadToCloudinary(file, { onProgress }) performing an unsigned upload:\n\n  POST https://api.cloudinary.com/v1_1/{VITE_CLOUDINARY_CLOUD_NAME}/image/upload\n\n  FormData: file, upload_preset = VITE_CLOUDINARY_UPLOAD_PRESET, folder = "gorebalance/payments"\n\n  Use XMLHttpRequest (not fetch) so upload.onprogress can report percentage.\n\n  Return { secure_url, public_id, bytes, format, width, height }.\n\n  Throw typed errors for: FILE_TOO_LARGE, UNSUPPORTED_TYPE, NETWORK_ERROR, UPLOAD_REJECTED.\n\n3) .env.example listing every variable above, with these values as defaults:\n\n  VITE_CLOUDINARY_CLOUD_NAME=dcoimqi\n\n  VITE_CLOUDINARY_UPLOAD_PRESET=levelupingup\n\n  (Firebase values from the existing project config.)\n\n  Add a comment: these must be added to Vercel's environment variables before deployment.\n\n4) SHARED FORM PRIMITIVES in src/components/assessment/fields/ — build once; later prompts must reuse these and never restyle inputs ad hoc:\n\n  FormField (label + required marker + helper text + error + children), TextInput, NumberInput, TextArea (optional character counter), PhoneInput (fixed +91 prefix affix), SelectField (shadcn Select — not a native select), RadioPills (pill-style radio group with visually-hidden real inputs and a fieldset/legend), CheckboxField, CheckboxGrid, ScaleSlider (1–10 with labelled endpoints), TimeInput.\n\nSHARED STYLING:\n\n  Label 13.5px/600 --text, 8px below. Required marker: an --accent asterisk, aria-hidden, with the input marked required.\n\n  Control: background --bg, 1.5px solid --border, radius 14px, padding 14px 16px, 15px, colour --text, height 52px (textarea min-height 130px, resize vertical only), transition 220ms.\n\n  Focus: border --primary, box-shadow 0 0 0 4px --primary-soft, outline none.\n\n  Error: border #DC2626 (light) / #F87171 (dark), box-shadow 0 0 0 4px rgba(220,38,38,0.10).\n\n  Placeholder --text-muted at 65%.\n\n  Helper text 12.5px --text-muted, margin-top 7px. Error text 12.5px in the error colour with a 13px AlertCircle, animating in over 200ms (opacity + translateY 4px).\n\n  Field spacing 24px.\n\nAll primitives accept react-hook-form registration and set aria-invalid and aria-describedby correctly.\n\nVerify: field borders meet 3:1 and all text 4.5:1 in BOTH themes; inputs never below 48px height; no horizontal scroll at 360px.\n\nBuild only this.`

  return (
    <div className="pt-32 px-12 whitespace-pre-wrap">
      {content}
    </div>
  )
}
